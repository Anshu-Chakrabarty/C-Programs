#include <stdio.h>

int main(){
    int a[10];
    int b;
    int i;
    
    printf("Enter the array");
    scanf("%d",a[i]);
    printf("Enter the search");
    scanf("%d",&b);
     int count= (sizeof(a[i])/sizeof(a[0]));
for (i = 0; i < count; i++)
{
    if (a[i]=b)
    {        printf("%d",i);
    }
}
return 0;
}